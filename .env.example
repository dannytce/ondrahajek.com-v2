DATOCMS_API_TOKEN=
# Content Management API (create/update records). Use Admin area → Settings → API tokens → Custom token with CMA access. Required for: pnpm import:portfolio
DATOCMS_MANAGEMENT_API_TOKEN=
DATOCMS_PREVIEW_SECRET=

