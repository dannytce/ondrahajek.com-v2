---
import { getDesktopNavLinkLabel, type Locale } from '~/i18n'

interface Props {
  locale: Locale
  index: number
  fallback: string
}

const { locale, index, fallback } = Astro.props
---

{getDesktopNavLinkLabel(locale, index, fallback)}
