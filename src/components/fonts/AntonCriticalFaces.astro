---
/**
 * Inlined Anton (subset WOFF2 as data URLs). Regenerate CSS with:
 * `pnpm generate:fonts` (see scripts/generate-font-subset/README.md).
 */
import antonCriticalCss from './anton-critical.generated.css?raw'
---

<style is:inline set:html={antonCriticalCss.trim()} />
